---
// TestimonialLightbox: Modal zoom foto testimoni interaktif (Zero Dependencies)
---

<div 
  id="testimonial-lightbox" 
  class="fixed inset-0 z-[9999] flex items-center justify-center p-3 sm:p-6 bg-black/90 backdrop-blur-md opacity-0 pointer-events-none transition-all duration-200"
  role="dialog"
  aria-modal="true"
  aria-label="Zoom Testimoni"
>
  <!-- Backdrop Click Area -->
  <div id="lightbox-backdrop" class="absolute inset-0 cursor-zoom-out" aria-hidden="true"></div>

  <!-- Modal Content Container -->
  <div class="relative z-10 max-w-4xl w-full max-h-[95vh] flex flex-col items-center justify-center scale-95 transition-transform duration-200" id="lightbox-card">
    
    <!-- Top Bar: Close Button & Category (Light Theme for high visibility) -->
    <div class="w-full flex items-center justify-between pb-3 text-white">
      <div class="flex items-center gap-2">
        <span id="lightbox-category" class="px-3.5 py-1 bg-white text-zinc-900 rounded-full text-xs font-bold tracking-wider uppercase shadow-md border border-zinc-200">
          WhatsApp
        </span>
        <span id="lightbox-counter" class="text-xs font-semibold text-white bg-black/50 backdrop-blur-xs px-2.5 py-0.5 rounded-full border border-white/20">
          1 / 6
        </span>
      </div>

      <button 
        id="lightbox-close" 
        class="w-10 h-10 rounded-full bg-white hover:bg-zinc-100 active:scale-95 transition-all flex items-center justify-center text-zinc-900 cursor-pointer border border-zinc-200 shadow-xl hover:scale-105"
        aria-label="Tutup pratinjau"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"></line>
          <line x1="6" y1="6" x2="18" y2="18"></line>
        </svg>
      </button>
    </div>

    <!-- Image Display Area -->
    <div class="relative w-full flex items-center justify-center overflow-hidden rounded-2xl bg-zinc-950/70 border border-white/10 shadow-2xl">
      <img 
        id="lightbox-img" 
        src="" 
        alt="Bukti Testimoni Klien" 
        class="max-h-[68vh] sm:max-h-[72vh] w-auto max-w-full object-contain select-none transition-opacity duration-150"
      />

      <!-- Navigation Arrows (Light Theme: Kontras tinggi di atas background gelap) -->
      <button 
        id="lightbox-prev" 
        class="absolute left-3 top-1/2 -translate-y-1/2 w-11 h-11 rounded-full bg-white hover:bg-zinc-100 active:scale-95 text-zinc-900 flex items-center justify-center transition-all cursor-pointer shadow-2xl hover:scale-105 border border-zinc-200"
        aria-label="Testimoni sebelumnya"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="15 18 9 12 15 6"></polyline>
        </svg>
      </button>

      <button 
        id="lightbox-next" 
        class="absolute right-3 top-1/2 -translate-y-1/2 w-11 h-11 rounded-full bg-white hover:bg-zinc-100 active:scale-95 text-zinc-900 flex items-center justify-center transition-all cursor-pointer shadow-2xl hover:scale-105 border border-zinc-200"
        aria-label="Testimoni berikutnya"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="9 18 15 12 9 6"></polyline>
        </svg>
      </button>
    </div>

    <!-- Bottom Caption Bar: Light Theme (Jelas & Kontras Tinggi) -->
    <div id="lightbox-caption" class="w-full mt-3 py-2.5 px-4 bg-white/95 backdrop-blur-md rounded-2xl border border-zinc-200 text-zinc-900 shadow-2xl text-center">
      <p id="lightbox-client" class="text-xs sm:text-sm font-bold text-zinc-900">
        Nama Klien • Profesi
      </p>
    </div>
  </div>
</div>

<script>
  (() => {
    const modal = document.getElementById('testimonial-lightbox');
    const modalContent = document.getElementById('lightbox-card');
    const backdrop = document.getElementById('lightbox-backdrop');
    const closeBtn = document.getElementById('lightbox-close');
    const prevBtn = document.getElementById('lightbox-prev');
    const nextBtn = document.getElementById('lightbox-next');
    const imgEl = document.getElementById('lightbox-img') as HTMLImageElement;
    const catEl = document.getElementById('lightbox-category');
    const counterEl = document.getElementById('lightbox-counter');
    const captionEl = document.getElementById('lightbox-caption');
    const clientEl = document.getElementById('lightbox-client');

    let currentItems: any[] = [];
    let currentIndex = 0;

    function updateModal(index: number) {
      if (!currentItems[index]) return;
      currentIndex = index;
      const data = currentItems[index];

      if (imgEl) {
        imgEl.style.opacity = '0.5';
        imgEl.src = data.image;
        imgEl.alt = data.clientName ? `Bukti Testimoni ${data.clientName}` : 'Bukti Testimoni Klien';
        imgEl.onload = () => { imgEl.style.opacity = '1'; };
      }

      if (catEl) catEl.textContent = data.category || 'Testimoni';
      if (counterEl) counterEl.textContent = `${currentIndex + 1} / ${currentItems.length}`;
      
      const clientInfo = [data.clientName, data.roleOrCompany].filter(Boolean).join(' • ');
      if (clientEl) {
        clientEl.textContent = clientInfo;
      }
      if (captionEl) {
        captionEl.style.display = clientInfo ? 'block' : 'none';
      }
    }

    function openModal(items: any[], startIndex: number) {
      currentItems = items;
      updateModal(startIndex);

      if (modal) {
        modal.classList.remove('opacity-0', 'pointer-events-none');
        modal.classList.add('opacity-100', 'pointer-events-auto');
      }
      if (modalContent) {
        modalContent.classList.remove('scale-95');
        modalContent.classList.add('scale-100');
      }
      document.body.style.overflow = 'hidden';
    }

    function closeModal() {
      if (modal) {
        modal.classList.remove('opacity-100', 'pointer-events-auto');
        modal.classList.add('opacity-0', 'pointer-events-none');
      }
      if (modalContent) {
        modalContent.classList.remove('scale-100');
        modalContent.classList.add('scale-95');
      }
      document.body.style.overflow = '';
    }

    function showPrev() {
      if (currentItems.length <= 1) return;
      const nextIdx = (currentIndex - 1 + currentItems.length) % currentItems.length;
      updateModal(nextIdx);
    }

    function showNext() {
      if (currentItems.length <= 1) return;
      const nextIdx = (currentIndex + 1) % currentItems.length;
      updateModal(nextIdx);
    }

    // Event Listeners
    closeBtn?.addEventListener('click', closeModal);
    backdrop?.addEventListener('click', closeModal);
    prevBtn?.addEventListener('click', (e) => { e.stopPropagation(); showPrev(); });
    nextBtn?.addEventListener('click', (e) => { e.stopPropagation(); showNext(); });

    document.addEventListener('keydown', (e) => {
      if (!modal || modal.classList.contains('pointer-events-none')) return;
      if (e.key === 'Escape') closeModal();
      if (e.key === 'ArrowLeft') showPrev();
      if (e.key === 'ArrowRight') showNext();
    });

    // Global hook for cards
    (window as any).__openTestimonialLightbox = (items: any[], index: number) => {
      openModal(items, index);
    };
  })();
</script>
